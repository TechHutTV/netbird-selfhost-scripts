# =============================================================================
# NetBird Self-Hosted with PocketID - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your deployment
# Usage: cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Domain Configuration
# -----------------------------------------------------------------------------
# Your NetBird domain (e.g., netbird.example.com)
NETBIRD_DOMAIN=netbird.example.com

# Your PocketID domain (e.g., auth.example.com)
POCKETID_URL=https://auth.example.com
# PocketID domain without protocol (extracted from POCKETID_URL, used by nginx)
POCKETID_DOMAIN=auth.example.com

# -----------------------------------------------------------------------------
# Secrets - Generate with: openssl rand -base64 32
# -----------------------------------------------------------------------------
# TURN server password (must match user password in turnserver.conf)
TURN_PASSWORD=generate-secure-password-here

# Relay authentication secret (must match NB_AUTH_SECRET in relay.env and Relay.Secret in management.json)
RELAY_AUTH_SECRET=generate-secure-password-here

# -----------------------------------------------------------------------------
# Optional: MaxMind GeoIP Database
# -----------------------------------------------------------------------------
# Sign up at https://www.maxmind.com/en/geolite2/signup for a free license key
# This enables geolocation features for peer connections
MAXMIND_LICENSE_KEY=
